# bstee615.github.io
My personal site, built using [Hugo](https://gohugo.io/).
Showcases personal projects and blogs I have time to write.

## Building and Deployment

To build a production version of the site, run `hugo`.
To serve a production version of the site, run `hugo serve`.

To display draft articles, add `-D` e.g. `hugo serve -D`.
